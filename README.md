# tcp_bridge
DLL Bridge TCP para uso com MT5 para envio de dados usando sockets


Compilar:
```cmd
cmake -S . -B build -A x64
cmake --build build --config Release

# DLL disponível em build/Release

```
